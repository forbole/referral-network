// All fbcli methods here

import { Meteor } from 'meteor/meteor';

let fbPath = Meteor.settings.fbcli.path;

Meteor.methods({
    // createAccount: (name, password) => {

    // },
    // sendCoin: (toAddr, amount, name, password, seq) => {}

    // Exec.run('ls', ['-al', '/*'], console.log(, stderrHandler);
});
